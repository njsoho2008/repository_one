
volatile 只能修饰 实例变量或者类变量，不能修饰方法或者方法的参数

volatile 关键字能够保证可见性  就是一旦有个线程修改了他修饰的变量的值，这个变量的改变会立即强制刷新到主内存

而不是在工作内存（可以这么认为就是CPU内部的高速缓存），其他线程要访问volatile修饰的变量的话，
不得不到主内存取访问修改后的值，

synchronized 关键字 是强排他功能  被他修饰的方法或者语句块 必定关联一个非空的对象这个对象是关联的monitor锁的
这个monitor锁 一定是强排他，迫使要要访问synchronized修饰的内容，一定是串行访问的。